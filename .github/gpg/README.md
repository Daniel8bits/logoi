# GPG public key

Place the release signing public key here as `logoi-release.pub`:

```bash
gpg --armor --export YOUR_KEY_ID > logoi-release.pub
```

See [SECRETS.md](../SECRETS.md) for setup instructions.
